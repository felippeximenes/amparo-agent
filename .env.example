# Nesta fase tudo roda local — este arquivo é opcional (os defaults do
# src/amparo/config.py já bastam). Copie para .env só se quiser sobrescrever.

QDRANT_PATH=./qdrant_data
QDRANT_COLLECTION=amparo_bpc
EMBEDDING_MODEL=intfloat/multilingual-e5-large
